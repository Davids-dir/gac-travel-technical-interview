{% extends 'base.html.twig' %}

{% block title %}Categorias{% endblock %}

{% block body %}
    {{ block('sidebar', 'product/index.html.twig') }}
    <!-- loader Start -->
    {#<div id="loading">
        <div id="loading-center">
        </div>
    </div>#}
    <!-- loader END -->
    <!-- Wrapper Start -->
    <div class="wrapper">
        <div class="content-page">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="d-flex flex-wrap flex-wrap align-items-center justify-content-between mb-4">
                            <div>
                                <h4 class="mb-3">Categorías</h4>
                            </div>
                            <a href="{{ path('new_category') }}" class="btn btn-primary add-list">
                                <i class="las la-plus mr-3"></i>Nueva categoría
                            </a>
                        </div>
                    </div>
                    <div class="col-lg-12">
                        <div class="table-responsive mb-3">
                            <table class="table mb-0 tbl-server-info no-footer" role="grid">
                                <thead class="bg-white text-uppercase">
                                <tr class="ligth ligth-data" role="row">
                                    <th class="text-left">Nombre</th>
                                    <th class="text-center">Código</th>
                                </tr>
                                </thead>
                                <tbody>
                                <tr>
                                    <td class="text-left">Balones de fútbol</td>
                                    <td class="text-center">11</td>
                                </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
